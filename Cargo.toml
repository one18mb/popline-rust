[package]
name = "popline-rust"
version = "0.1.0"
edition = "2021"
description = "PopLine serialization format for Rust"
license = "MIT"

[dependencies]
serde_json = "1.0"

[dev-dependencies]
serde = { version = "1.0", features = ["derive"] }
